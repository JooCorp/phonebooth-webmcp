{
  "name": "phonebooth",
  "private": true,
  "version": "0.1.0",
  "description": "A WebMCP front door for calls handled by JooVoice.",
  "license": "Apache-2.0",
  "author": "JooCorp Private Limited",
  "homepage": "https://booth.joovoice.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/JooCorp/phonebooth-webmcp.git"
  },
  "type": "module",
  "packageManager": "bun@1.3.6",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "start": "bun run --cwd apps/page dev",
    "dev": "bun run --cwd apps/page dev",
    "dev:page": "bun run --cwd apps/page dev",
    "build": "bun run --cwd apps/page build",
    "typecheck": "bun run --cwd packages/design typecheck && bun run --cwd packages/state-as-tools typecheck && bun run --cwd packages/sim-mcp typecheck && bun run --cwd apps/page typecheck",
    "test": "bun test",
    "test:local-e2e": "PHONEBOOTH_LOCAL_E2E=1 bun test apps/page/src/lib/integration/local-stack.e2e.test.ts",
    "check": "bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
